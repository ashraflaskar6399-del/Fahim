README.md steps:
Install Node.js + Android Studio
npm install in the folder
npm run build
npx cap add android
npx cap sync android
npx cap open android → click Run on your phone
The README.md inside the zip has a full troubleshooting table and a one-command shortcut (npm run deploy:android) for every rebuild after that.
